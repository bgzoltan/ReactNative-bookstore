import { Text } from "react-native";
import Screen from "../components/Screen";

export default function AccountListingScreen({ navigation }) {
  return (
    <Screen>
      <Text>AccountListingScreen</Text>
    </Screen>
  );
}
